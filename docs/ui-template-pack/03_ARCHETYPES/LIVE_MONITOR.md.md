# LIVE_MONITOR.md Archetype

Realtime page backed by backend fan-out. Must expose connected/stale/disconnected states and minimize render churn.

Use shared components and DESIGN.md. Page-specific requirements always override this generic archetype.
