# AUTH.md Archetype

Auth shell for login, MFA and password recovery. Minimal fields, clear errors, security events, no tenant leakage.

Use shared components and DESIGN.md. Page-specific requirements always override this generic archetype.
