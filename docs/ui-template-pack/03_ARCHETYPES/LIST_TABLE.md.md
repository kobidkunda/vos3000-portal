# LIST_TABLE.md Archetype

Dense operational table with server-side query controls, row actions, export and predictable empty/error states.

Use shared components and DESIGN.md. Page-specific requirements always override this generic archetype.
