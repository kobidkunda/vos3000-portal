# Frontend State
Use server state/query cache for API data, local component state for ephemeral controls, URL state for shareable filters. Do not duplicate canonical server data into global client stores without a clear reason.
