# Support With CDR/Gateway Context
Create ticket → choose category → optionally attach customer-safe CDR/gateway reference → store immutable support reference → operator thread → resolution. Never auto-attach hidden carrier/internal data to client-facing thread.
