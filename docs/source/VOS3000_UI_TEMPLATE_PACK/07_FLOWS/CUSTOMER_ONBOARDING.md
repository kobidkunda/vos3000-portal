# Customer Onboarding Flow
Admin create wizard → company/profile → portal owner → map/create VOS account → commercial settings → gateway defaults → permissions/API → review → create.
Use compensation/rollback if downstream creation partially fails.
