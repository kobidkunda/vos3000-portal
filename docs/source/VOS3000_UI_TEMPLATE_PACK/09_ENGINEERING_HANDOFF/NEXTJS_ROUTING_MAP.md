# Next.js Routing Map
Use App Router route groups such as `(admin)` and `(client)` while preserving public URLs from `ROUTE_MANIFEST.json`.
Dynamic parameters remain server-validated. Do not derive tenant authorization from URL parameters.
