# FilterBar
URL-addressable on list/report pages where practical. Supports search, date range, scoped selects, active-filter chips, clear-all, optional saved views. Never trigger unbounded large-data queries by clearing filters.
